export const initialState = {
    item: "",
    completed: false,
    id: 0
}

export const reducer = (state, action) => {
    switch (action.type) {
        default: 
            return state;
    }
}
