export const initialState = {
    todos: [
        {
            item: "Sample todo",
            completed: false,
            id: 0
        }
    ]
}

export const reducer = (state, action) => {
    switch (action.type) {
        default: 
            return state;
    }
}
